enum AppBarHeaders { projects, contact, aboutMe, termCondition, privacyPolicy, dmca }

enum AppBarHeadersAxis { horizontal, vertical }

enum DeviceType {
  mobile,
  ipad,
  smallScreenLaptop,
  largeScreenDesktop,
  extraLargeTV,
}
